import s from "./Footer.module.css"
import hs from "../Wrapper.module.css"

const Footer = () => {
  return (
    <footer className={`${s.footer} ${hs.footer}`}>
      Footer
    </footer>
  )
}

export default Footer;
